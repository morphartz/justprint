import {NextResponse,type NextRequest} from 'next/server';import {createServerClient} from '@supabase/ssr';
export async function middleware(req:NextRequest){if(!req.nextUrl.pathname.startsWith('/admin'))return NextResponse.next();let res=NextResponse.next({request:req});const sup=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{cookies:{getAll(){return req.cookies.getAll()},setAll(v){v.forEach(({name,value,options})=>res.cookies.set(name,value,options))}}});const{data:{user}}=await sup.auth.getUser();if(!user)return NextResponse.redirect(new URL('/account?next=/admin',req.url));const{data:profile}=await sup.from('user_profiles').select('is_admin').eq('id',user.id).single();if(!profile?.is_admin)return NextResponse.redirect(new URL('/',req.url));return res}
export const config={matcher:['/admin/:path*']};
