'use client';import {FormEvent,useState} from 'react';import {useCart} from '@/components/cart';import {useRouter} from 'next/navigation';
export default function Checkout(){const{items,total}=useCart();const router=useRouter();const[name,setName]=useState('');const[email,setEmail]=useState('');const[phone,setPhone]=useState('');const[address,setAddress]=useState('');const[city,setCity]=useState('Algiers');const[wilaya,setWilaya]=useState('Algiers');const[note,setNote]=useState('');const[done,setDone]=useState('');async function submit(e:FormEvent){e.preventDefault();if(!items.length)return;const order=`JP-${Date.now().toString().slice(-8)}`;localStorage.setItem('jp-last-order',JSON.stringify({order,name,email,phone,address,city,wilaya,note,items,total,createdAt:new Date().toISOString()}));setDone(order);setTimeout(()=>router.push('/'),1800)}if(done)return <main className="section"><h1>Order confirmed.</h1><p>Your order <strong>{done}</strong> has been saved.</p><p className="muted">We’ll contact you to confirm delivery and payment.</p></main>;return <main className="checkout"><h1>Checkout</h1><form onSubmit={submit} className="panel"><div className="formgrid"><input required className="input" placeholder="Full name" value={name} onChange={e=>setName(e.target.value)}/><input required type="email" className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input required className="input" placeholder="Phone" value={phone} onChange={e=>setPhone(e.target.value)}/><input required className="input" placeholder="City" value={city} onChange={e=>setCity(e.target.value)}/><input required className="input full" placeholder="Delivery address" value={address} onChange={e=>setAddress(e.target.value)}/><input required className="input" placeholder="Wilaya" value={wilaya} onChange={e=>setWilaya(e.target.value)}/><input className="input" placeholder="Delivery notes (optional)" value={note} onChange={e=>setNote(e.target.value)}/></div><div className="summary"><div className="summaryline"><span>Total</span><strong>{total} DA</strong></div><button className="btn" style={{width:'100%'}}>Place order · Cash on delivery</button></div></form></main>}
