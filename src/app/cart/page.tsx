'use client';import Link from 'next/link';import Image from 'next/image';import {useCart} from '@/components/cart';
export default function Cart(){const{items,remove,update,total}=useCart();if(!items.length)return <main className="section"><h1>Your cart is empty.</h1><p className="muted">Humanity survived without a cart for centuries. You can fill this one.</p><Link className="btn dark" href="/shop">Shop now</Link></main>;return <main className="checkout"><p className="muted">YOUR CART</p><h1>{items.length} item{items.length>1?'s':''}</h1>{items.map(i=><div className="cartrow" key={i.id}><Image src={i.image} alt={i.name} width={90} height={110}/><div><strong>{i.name}</strong><p className="muted">{i.color} · {i.size}{i.custom?.text&&` · “${i.custom.text}”`}</p><button onClick={()=>remove(i.id)} className="muted" style={{border:0,background:'none',padding:0}}>Remove</button></div><div><div>{i.price*i.qty} DA</div><div className="qty"><button onClick={()=>update(i.id,i.qty-1)}>−</button><span>{i.qty}</span><button onClick={()=>update(i.id,i.qty+1)}>+</button></div></div></div>)}<div className="summary"><div className="summaryline"><span>Subtotal</span><strong>{total} DA</strong></div><div className="summaryline"><span>Delivery</span><span>Calculated at checkout</span></div><div className="summaryline"><span>Total</span><strong>{total} DA</strong></div><Link className="btn" style={{marginTop:18,width:'100%'}} href="/checkout">Checkout</Link></div></main>}
