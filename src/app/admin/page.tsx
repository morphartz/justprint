'use client';import {useEffect,useState} from 'react';import Link from 'next/link';
export default function Admin(){const[orders,setOrders]=useState<any[]>([]);useEffect(()=>{const o=localStorage.getItem('jp-last-order');if(o)setOrders([JSON.parse(o)])},[]);return <main className="admin"><p className="muted">BACK OFFICE</p><h1>JUST PRINT admin</h1><div className="adminbox"><h2>Quick actions</h2><div className="actions" style={{justifyContent:'flex-start'}}><Link className="btn dark" href="/shop">View store</Link><Link className="btn" href="/customize">Test customizer</Link></div></div><div className="adminbox"><h2>Recent orders</h2>{orders.length?<div>{orders.map(o=><div className="adminrow" key={o.order}><strong>{o.order}</strong><span>{o.name}</span><span>{o.total} DA</span></div>)}</div>:<p className="muted">No orders in this browser yet.</p>}</div><div className="adminbox"><h2>Catalog</h2><p className="muted">The starter catalog is defined in <code>src/lib/products.ts</code>. Connect the included Supabase schema for persistent multi-device inventory and order management.</p></div></main>}
