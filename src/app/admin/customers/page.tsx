'use client';import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';
export default function Customers(){const[d,setD]=useState<any[]>([]);useEffect(()=>{supabase().from('user_profiles').select('*').order('created_at',{ascending:false}).limit(200).then(({data})=>setD(data||[]))},[]);return <main className="section"><p className="eyebrow">ADMIN / CUSTOMERS</p><h1>CUSTOMERS.</h1><div className="list">{d.map(x=><article className="panel row" key={x.id}><div><strong>{x.full_name||'Customer'}</strong><p className="muted">{x.email} · {x.phone||'No phone'}</p></div><span>{x.is_admin?'ADMIN':'CUSTOMER'}</span></article>)}</div></main>}
