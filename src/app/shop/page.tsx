import Image from 'next/image';import Link from 'next/link';import {products} from '@/lib/products';
export default function Shop(){return <main className="section"><p className="muted">JUST PRINT / SHOP</p><h1>Everything starts with a good blank.</h1><p className="muted">Premium cotton bases, ready for personalisation.</p><div className="grid" style={{marginTop:40}}>{products.map(p=><Link className="card" href={'/product/'+p.slug} key={p.id}><Image src={p.image} alt={p.name} width={700} height={900}/><h3>{p.name}</h3><p className="muted">{p.material}</p><span className="price">{p.salePrice? <><span className="old">{p.price} DA</span>{p.salePrice} DA</>:p.price+' DA'}</span></Link>)}</div></main>}
